

# About

Directory contains programs (mostly configurations) related to the development of this application.

E.g. inside 'fedora' is a mounted volume to the docker container launched via docker-compose at root. (+ configuration mounted into cointainer + data mounted back visible here in vscode)

Inside "insomnia" you'll find an export of API testing scenarios for fedora6 like "POST a resource" 

